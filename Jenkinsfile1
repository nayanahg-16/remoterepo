pipeline {
    agent any

    stages {
        stage('hostname') {
            steps {
                sh 'hostname'
            }
        }

        stage('date') {
            steps {
                sh 'date'
            }
        }
    }
}
